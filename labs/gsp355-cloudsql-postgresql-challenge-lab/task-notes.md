# GSP355 Task Notes

Use the current lab page as the source of truth for the randomized task values.

Record locally (do not commit secrets):
- Project ID
- Instance name
- Region
- PostgreSQL version
- Database name
- Database username
- Required tier/settings
- Any additional challenge-specific operations

The helper intentionally asks for these values instead of embedding credentials or stale challenge values.
