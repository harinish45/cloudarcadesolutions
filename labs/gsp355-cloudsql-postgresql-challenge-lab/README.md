# GSP355 Cloud SQL PostgreSQL Challenge Lab

Clone and run from Google Cloud Shell:

```bash
git clone https://github.com/harinish45/cloudarcadesolutions.git
cd cloudarcadesolutions/labs/gsp355-cloudsql-postgresql-challenge-lab
bash setup.sh
```

This helper automates the repeatable Cloud SQL work for GSP355 while keeping task-specific values interactive. Challenge labs can randomize names, regions and requirements, so the script does not hard-code credentials or claim to bypass the lab grader.

Run `bash verify.sh` after setup.
